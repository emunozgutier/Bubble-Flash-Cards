{
  "hash": "cdbdb3b",
  "date": "2026-06-26T02:02:07-07:00",
  "title": "Enforce unique key mappings across actions",
  "message": "Previously the code only prevented duplicates within the same action. This change removes the pressed key code from all actions first, then adds it to the target action so a key can only be mapped to a single action. Updates use a cleaned mappings object and then call setMappings/saveMappings with the new state.",
  "source": "Local Git"
}